spring:
  datasource:
    url: jdbc:mysql://localhost/sourcefx
    username: root
    password: 123456
  jpa:    hibernate:      ddl-auto: update
  data:
    web:
      pageable:
        one-indexed-parameters: true